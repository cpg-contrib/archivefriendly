When a user uploads an image and an archive with identical names, the image will
render an href direct to the archive.

The entire path needs to be identical, take this into account if you are batch adding.